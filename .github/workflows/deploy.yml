name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Clean workspace
        run: |
          rm -rf ./* || true
          rm -rf ./.* || true

      - name: Initialize Next.js
        run: npx create-next-app nearforceweb --ts --tailwind --eslint --app --no-src-dir --import-alias "@/*" --use-npm --yes

      - name: Move files to root
        run: |
          mv nearforceweb/* .
          mv nearforceweb/.* . || true
          rm -rf nearforceweb

      - name: Install additional dependencies
        run: |
          npm install framer-motion lucide-react
          npm install -D shadcn

      - name: Create directory structure
        run: |
          mkdir -p lib
          mkdir -p components/ui

      - name: Setup shadcn configuration
        run: |
          echo '{
            "$schema": "https://ui.shadcn.com/schema.json",
            "style": "default",
            "rsc": false,
            "tsx": true,
            "tailwind": {
              "config": "tailwind.config.ts",
              "css": "app/globals.css",
              "baseColor": "slate",
              "cssVariables": true
            },
            "aliases": {
              "components": "@/components",
              "utils": "@/lib/utils"
            }
          }' > components.json

      - name: Create utility functions
        run: |
          echo 'import { type ClassValue, clsx } from "clsx"
          import { twMerge } from "tailwind-merge"
          
          export function cn(...inputs: ClassValue[]) {
            return twMerge(clsx(inputs))
          }' > lib/utils.ts

      - name: Add UI components
        run: |
          npx shadcn@latest add button --yes
          npx shadcn@latest add card --yes
          npx shadcn@latest add input --yes
          npx shadcn@latest add textarea --yes

      - name: Update next.config
        run: |
          echo 'module.exports = {
            output: "export",
            images: {
              unoptimized: true,
            },
            basePath: "/NearForceWeb",
            assetPrefix: "/NearForceWeb/",
          }' > next.config.js

      - name: Verify package.json
        run: |
          if [ ! -f "package.json" ]; then
            echo '{"name":"nearforceweb","version":"0.1.0","private":true}' > package.json
          fi
          npm pkg set name="nearforceweb"

      - name: Build
        run: |
          npm run build
          touch out/.nojekyll

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out